services:
  db:
    image: pgvector/pgvector:pg16
    container_name: contextual_rag_db
    restart: unless-stopped
    environment:
      POSTGRES_USER: rag
      POSTGRES_PASSWORD: rag
      POSTGRES_DB: contextual_rag
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U rag -d contextual_rag"]
      interval: 5s
      timeout: 5s
      retries: 10

volumes:
  pgdata: